# Legacy tldraw canvas data

This folder previously stored the old tldraw whiteboard snapshots
(for example `default.json`).

Status: **LEGACY — do not use in the React Flow workflow editor.**

- Do **not** convert these files into WorkflowDocument.
- Do **not** delete until the project owner explicitly confirms.
- The active workflow store is `data/workflows/`.
