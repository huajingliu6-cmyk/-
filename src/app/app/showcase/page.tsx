import { ModulePlaceholder } from "@/shell/ModulePlaceholder";

export default function ShowcasePage() {
  return (
    <ModulePlaceholder
      title="作品展示"
      description="作品展示模块尚未接入。后续将在此浏览已生成成片与分享入口。"
    />
  );
}
